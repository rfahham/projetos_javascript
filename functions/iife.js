// Immediateky Invoked Function Expressions
// Executed immediately after their creation.
// Used to create private scopes and avoid polluting the global namespace.

(function () {
	let str = "Ricardo Fahham";
	console.log(str);	
})();
